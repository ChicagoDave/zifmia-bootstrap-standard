'use strict';

/**
 * @ngdoc function
 * @name zbsApp.controller:MapsCtrl
 * @description
 * # MapsCtrl
 * Controller of the zbsApp
 */
angular.module('zbsApp')
  .controller('MapsCtrl', function ($scope) {
    $scope.pageClass = 'page-maps';
    $scope.viewName = 'Maps';
  });
